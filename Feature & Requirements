Feature:
- Checks for:
  - Minimum length of **8 characters**
  - At least **1 uppercase letter**
  - At least **1 number**
  - At least **1 special character** (e.g., `!@#$%^&*`)
- Uses Python’s built-in `re` (regex) module
- Command-line interface for easy testing and use
Requirements:
- Python 3.x
